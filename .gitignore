# Python
__pycache__/
*.pyc
.venv/
venv/

# Atlas runtime data (keep structure, ignore state)
data/portfolio.json
data/journal.json
data/ideas.json
data/broker_state.json

# Logs
logs/*.log
*.log

# Secrets — NEVER commit real broker keys
config.local.yaml
*.secret

# access control runtime state
data/access.json
data/.lastseen.json

# improvement suggestions + inbound monitor state
data/improvements.json
data/.alerts_pending.jsonl
data/inbound_log.jsonl
data/.seen_mail.json
data/digests/

# local secrets (NEVER commit)
config.local.yaml

# cloudflared named-tunnel creds (gitignored)
.cloudflared/

# tunnel url state (gitignored)
data/.tunnel_url

# alpaca mcp (gitignored)
.env.alpaca_mcp
.alpaca_mcp/
